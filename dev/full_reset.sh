#!/bin/bash

This script resets the dev environment to a pristine state, and configures the the app to run in default-disabled mode, where all feeds will be switched off by default.

This allows devs to selectively switch on individual feeds that need testing.
